import 'package:flutter/material.dart';

class DetailFaqScreen extends StatelessWidget {
  const DetailFaqScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}